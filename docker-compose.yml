version: '3.8'

services:
  tts-api:
    build: .
    container_name: microvox-tts
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
    env_file:
      - .env
    restart: unless-stopped

